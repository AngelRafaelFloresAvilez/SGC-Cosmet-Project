# Optional: provide real SMTP credentials to use your SMTP provider
# If left blank, the server will use an Ethereal test account (no credentials required)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="SGC Cosmetic <no-reply@example.com>"
PORT=4000
