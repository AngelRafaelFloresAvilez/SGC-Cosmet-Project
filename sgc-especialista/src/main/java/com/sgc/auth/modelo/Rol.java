package com.sgc.auth.modelo;

// Esto antes era un "enum". Como en clase solo hemos visto clases normales,
// lo dejamos como una clase con una constante de texto (mismo resultado, mas facil de leer).
public class Rol {
    public static final String ESPECIALISTA = "ESPECIALISTA";
}
